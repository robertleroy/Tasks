# Tasks


    A tasks app persisted to a sqLite database..
    .. served over a Tailscale network


```sh
# In development, when you change schema:
npx drizzle-kit generate  # Creates migration SQL files
npx drizzle-kit migrate   # Applies migrations to your DB
```






